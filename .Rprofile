# Configuration R pour le projet
# Ce fichier est exécuté automatiquement au démarrage de R dans ce projet

# Activer le renv si disponible
if (file.exists("renv/activate.R")) {
  source("renv/activate.R")
}
